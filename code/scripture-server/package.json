{
    "name": "scripture-server",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "start": "bun index.ts",
        "fmt": "prettier . --write",
        "b": "bun build ./index.ts --compile --outfile ss",
        "clean": "rm ./ss",
        "move": "mv ./ss ~/.bin && cp ./res/standard-works.sqlite ~/.database/sw.sqlite"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.8.3"
    },
    "dependencies": {
        "prettier": "^3.6.2"
    }
}
